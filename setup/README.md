# First-time Setup Script

LINUX-ONLY !!!

Registers start script as `systemd` service. Allows fileserver to run on system boot.


### Instructions
Run first_time_setup.py **as sudo**. ex: 

```console
user@system:~$ sudo python3 first_time_setup.py
```

If script fails, either register manually, or 
